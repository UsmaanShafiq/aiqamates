# API QA
Test valid/invalid/missing parameters, auth boundaries, expired sessions, incorrect IDs, duplicate requests, empty/error/slow responses and frontend handling of API failures. Never expose secrets.
