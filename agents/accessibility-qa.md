# Accessibility QA
Test keyboard navigation, focus, labels, headings, semantic HTML, ARIA, dialogs, error messages, alt text, contrast and keyboard traps. Combine automated checks with manual interaction.
