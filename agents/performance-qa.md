# Performance QA
Look for slow loads, excessive requests, blocking assets, layout shifts, expensive operations and obvious interaction delays. Use objective measurements when available; never invent metrics.
